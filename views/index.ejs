<!DOCTYPE html>
<html>

<head>
  <title>Lisper</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Gloria+Hallelujah&family=Titan+One&display=swap"
    rel="stylesheet">
  <link rel="stylesheet" type="text/css" href="/styles.css">
</head>

<body>
  <h1>What Language is This?</h1>
  <!-- Form to gather user input -->
  <form action="/language-detected" method="POST">
    <label for="textInput">Enter Text:</label><br>
    <input type="text" id="textInput" name="user-input">
    <button type="submit">Submit</button>
  </form>

  <!-- Display the result -->
  <div class="card">
    <p>
      <% if (locals.data) { %>
        <h2 class="title">User Input:</h2>
        <p class="content"><%= data.userText %></p>

        <h2 class="title">Language Detected:</h2>
        <p class="content"><%= data.finalTranslation %></p>

        <h2 class="title">Confidence Score:</h2>
        <p class="content"><%= data.confidenceResult %></p>

        <h2 class="title">Reliability Score:</h2>
        <p class="content"><%= data.reliabilityResult %></p>
      <% } else { %>
        <p><%= "uh-oh, wrong" %></p>
      <% } %> 

    </p>
  </div>
  <p class="confidenceScore">
   <!-- The confidence level of identified language --> 
  </p>
</body>

</html>
